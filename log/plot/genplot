prefix=errorbars
#prefix=histo
#prefix=boxerror

script=$prefix.p
output=$prefix.png

#git rm $output
#git commit -a -m $prefix
#git push

gnuplot $script

git add .
git commit -a -m $prefix
git push
